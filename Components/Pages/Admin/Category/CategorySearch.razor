<!-- Thanh lọc -->
<div class="d-flex justify-content-between align-items-start mb-3 flex-wrap gap-2">
    <div class="d-flex flex-wrap gap-2 align-items-center flex-grow-1">
        <div class="input-group input-group-sm" style="width: 300px;">
            <input type="text" class="form-control" placeholder="Nhập tên loại sản phẩm" 
                id="searchInput"
                @bind="KeyWord">
            <button class="btn btn-primary btn-search" 
                type="button" id="searchBtn"
                    @onclick="HandleSearch">
                <i class="bi bi-search"></i>
            </button>
        </div>
    </div>
    <button class="btn btn-primary btn-sm btn-add" data-bs-toggle="modal" data-bs-target="#categoryModal">
        <i class="bi bi-plus-circle"></i> Thêm mới
    </button>
</div>
@code {
    [Parameter]
    public string KeyWord { get; set; }

    [Parameter]
    public EventCallback<string> OnSearch { get; set; }
    private async Task HandleSearch()
    {
        await OnSearch.InvokeAsync(KeyWord);
    }
}
