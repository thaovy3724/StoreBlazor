@page "/client/payment-fail/{OrderId:int}"
@layout ClientLayout

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="card shadow-lg border-0">
                <div class="card-body text-center p-5">

                    <!-- Icon -->
                    <div class="mb-4">
                        <i class="bi bi-x-circle-fill text-danger" style="font-size: 4rem;"></i>
                    </div>

                    <!-- Title -->
                    <h3 class="fw-bold text-danger mb-3">Thanh toán thất bại!</h3>

                    <p class="fw-semibold text-dark">
                        Mã đơn hàng: <span class="text-primary">#@OrderId</span>
                    </p>

                    <!-- Message -->
                    <p class="text-muted mb-4">
                        Rất tiếc, giao dịch thanh toán của bạn chưa được hoàn tất.
                        Có thể do bạn hủy giao dịch, kết nối mạng không ổn định
                        hoặc lỗi từ phía cổng thanh toán.
                    </p>

                    <!-- Actions -->
                    <div class="d-flex justify-content-center gap-3">
                        <!-- Thử thanh toán lại -->
                        <a href="/client/checkout" class="btn btn-outline-danger px-4 py-2">
                            <i class="bi bi-arrow-repeat me-2"></i>
                            Thử thanh toán lại
                        </a>

                        <!-- Về trang chủ -->
                        <a href="/client" class="btn btn-secondary px-4 py-2">
                            <i class="bi bi-house-door-fill me-2"></i>
                            Về trang chủ
                        </a>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>

@code {
    [Parameter] public int OrderId { get; set; }
}
